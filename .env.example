# Coolify-specific variable for the Grafana service FQDN
SERVICE_FQDN_GRAFANA_3000=grafana.apps.your-domain.com

# Grafana Admin Password
SERVICE_PASSWORD_GRAFANA=change-this-to-a-strong-password

# PostgreSQL Credentials
SERVICE_USER_POSTGRES=grafana
SERVICE_PASSWORD_POSTGRES=change-this-to-a-strong-password
POSTGRES_DB=grafana

# Google Cloud Service Account for Loki (paste the full JSON content)
# Make sure to wrap the JSON in single quotes if it contains special characters.
GCS_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'

# GCS Bucket Name for Loki and Tempo
GCS_BUCKET_NAME=your-gcs-bucket-name

# Public URL for the Alloy OTLP endpoint
SERVICE_FQDN_ALLOY_4318=alloy.apps.your-domain.com

# Basic Auth credentials for the Alloy endpoint
# Generate with: htpasswd -nbB <user> <password>
# Example for user 'admin' and password 'admin':
# ALLOY_BASIC_AUTH=admin:$2y$05$1bO/U1FqL9j/6iS.D.gMGe2M.s3gT6w0.i.M/kE3a5i.pGz1e4G.K
ALLOY_BASIC_AUTH=your-htpasswd-string-here
