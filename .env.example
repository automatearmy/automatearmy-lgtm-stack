# -----------------------------------------------------------------------------
# Coolify Deployment Settings
# These variables are primarily used by Coolify to expose services.
# -----------------------------------------------------------------------------

# Public URL for the Grafana UI
SERVICE_FQDN_GRAFANA_3000=grafana.apps.your-domain.com

# Public URL for the Alloy OTLP endpoint (for receiving traces)
SERVICE_FQDN_ALLOY_4318=alloy.apps.your-domain.com

# -----------------------------------------------------------------------------
# Service Credentials
# -----------------------------------------------------------------------------

# Grafana Admin User Password
SERVICE_PASSWORD_GRAFANA=change-this-to-a-strong-password

# PostgreSQL Credentials (used by Grafana)
POSTGRES_DB=grafana
SERVICE_USER_POSTGRES=grafana
SERVICE_PASSWORD_POSTGRES=change-this-to-a-strong-password

# Basic Auth for the Alloy endpoint
# Protects the public OTLP receiver from unauthorized access.
# Generate with: htpasswd -nbB <user> <password>
# Example for user 'admin' and password 'admin':
# ALLOY_BASIC_AUTH=admin:$2y$05$1bO/U1FqL9j/6iS.D.gMGe2M.s3gT6w0.i.M/kE3a5i.pGz1e4G.K
ALLOY_BASIC_AUTH=your-htpasswd-string-here

# -----------------------------------------------------------------------------
# Google Cloud Storage (GCS) Configuration
# -----------------------------------------------------------------------------

# GCS Bucket Name for Loki (logs) and Tempo (traces)
GCS_BUCKET_NAME=your-gcs-bucket-name

# Google Cloud Service Account JSON
# Paste the entire content of your service account key file here.
# IMPORTANT: It is recommended to use single quotes to wrap the JSON to
# prevent shell expansion issues with special characters.
GCS_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'